import { Container, styled } from "@mui/material";

export const StyledContainer = styled(Container)({
  width: "100%",
  maxWidth: "1280px",
  paddingBlock: "2rem",
  margin: "0 auto",
  textAlign: "center",
});
